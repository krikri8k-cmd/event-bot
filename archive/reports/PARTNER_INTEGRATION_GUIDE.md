# 🤝 Руководство по интеграции с организаторами событий

## 📋 Что нужно запросить у организатора

### 1. **Источник данных о событиях** (выберите один вариант):

#### ✅ Вариант A: ICS календарь (рекомендуется)
**Что запросить:**
- URL публичного ICS календаря (например: `https://example.com/events.ics`)
- Или приватный календарь с токеном доступа

**Преимущества:**
- Стандартный формат (iCalendar)
- Автоматическое обновление
- Простая интеграция

**Пример запроса:**
```
"Можете предоставить публичный ICS календарь со всеми вашими событиями? 
Нам нужен URL вида: https://ваш-сайт.com/events.ics"
```

#### ✅ Вариант B: API endpoint
**Что запросить:**
- URL API endpoint
- Метод аутентификации (API key, OAuth, токен)
- Формат данных (JSON, XML)
- Документация API

**Пример запроса:**
```
"Есть ли у вас API для получения списка событий? 
Нужен endpoint, который возвращает JSON с событиями (название, дата, место, ссылка)"
```

#### ✅ Вариант C: RSS feed
**Что запросить:**
- URL RSS ленты событий

**Пример запроса:**
```
"Можете предоставить RSS feed со всеми вашими событиями?"
```

### 2. **Реферальная ссылка/код**

**Что запросить:**
- Реферальный код (например: `REF123` или `PARTNER2024`)
- Или полная реферальная ссылка с параметром (например: `?ref=PARTNER2024` или `?affiliate=REF123`)
- Формат параметра (может быть `ref`, `affiliate`, `utm_source`, `partner` и т.д.)

**Примеры форматов:**
```
https://example.com/event/123?ref=PARTNER2024
https://example.com/event/123?affiliate=REF123
https://example.com/event/123?utm_source=eventbot&utm_medium=partner
```

**Пример запроса:**
```
"Какой формат реферальной ссылки вы используете? 
Нам нужно добавлять реферальный код ко всем ссылкам на ваши события, 
чтобы отслеживать переходы из нашего бота."
```

### 3. **Дополнительная информация** (опционально):

- **Частота обновления:** Как часто обновляются события? (каждый час, день, неделю?)
- **Геолокация:** Есть ли координаты (lat/lng) для событий или только адреса?
- **Изображения:** Есть ли обложки/фото событий?
- **Категории:** Какие категории событий у вас есть?

---

## 🔧 Техническая реализация

### Как добавить источник организатора:

#### 1. **Если у организатора есть ICS календарь:**

**Шаг 1:** Примените миграцию для создания таблицы реферальных кодов:

```sql
-- Создать таблицу partner_referral_codes
psql $DATABASE_URL -f sql/2025_partner_referral_codes.sql
```

**Шаг 2:** Добавьте реферальный код в таблицу `partner_referral_codes`:

```sql
INSERT INTO partner_referral_codes (
    source_url,           -- URL ICS календаря
    referral_code,        -- Реферальный код
    referral_param,       -- Название параметра ('ref', 'affiliate', 'utm_source' и т.д.)
    partner_name          -- Название партнёра
) VALUES (
    'https://organizer.com/events.ics',
    'REF123',
    'ref',
    'Название организатора'
);
```

**Шаг 3:** Добавьте URL календаря в переменную окружения `ICS_FEEDS`:

```bash
ICS_FEEDS=https://organizer.com/events.ics
ENABLE_ICS_FEEDS=1
```

#### 2. **Автоматическое добавление реферального кода:**

Система автоматически найдёт реферальный код в таблице и добавит его ко всем URL событий при парсинге.

**Настройка реферального кода для источника:**

Система автоматически добавляет реферальный код ко всем URL событий из источника.

**Обновление существующего реферального кода:**

```sql
-- Обновить реферальный код для источника
UPDATE partner_referral_codes 
SET referral_code = 'REF123',
    referral_param = 'ref'  -- или 'affiliate', 'utm_source' и т.д.
WHERE source_url = 'https://organizer.com/events.ics';
```

**Применение миграции:**

Сначала примените миграцию для создания таблицы:

```bash
# Применить миграцию
psql $DATABASE_URL -f sql/2025_partner_referral_codes.sql
```

---

## 📝 Пример интеграции

### Сценарий: Организатор предоставил ICS календарь

1. **Получаем от организатора:**
   - ICS URL: `https://organizer.com/events.ics`
   - Реферальный код: `EVENTBOT2024`

2. **Добавляем в систему:**

   **a) Создайте таблицу (если ещё не создана):**
   ```sql
   psql $DATABASE_URL -f sql/2025_partner_referral_codes.sql
   ```

   **b) Добавьте реферальный код:**
   ```sql
   INSERT INTO partner_referral_codes (
       source_url, referral_code, referral_param, partner_name
   ) VALUES (
       'https://organizer.com/events.ics',
       'EVENTBOT2024',  -- реферальный код
       'ref',           -- название параметра
       'Организатор X'  -- название партнёра
   );
   ```

   **c) Добавьте URL в переменные окружения:**
   ```bash
   ICS_FEEDS=https://organizer.com/events.ics
   ENABLE_ICS_FEEDS=1
   ```

3. **Система автоматически:**
   - Загружает события из ICS календаря каждый час
   - Добавляет реферальный код `?ref=EVENTBOT2024` ко всем URL событий
   - Сохраняет события в базу данных
   - Показывает события пользователям бота

4. **Пользователь видит событие:**
   - Нажимает на ссылку "Источник"
   - Переходит на `https://organizer.com/event/123?ref=EVENTBOT2024`
   - Организатор видит, что переход пришёл из нашего бота

---

## 🎯 Чек-лист для переговоров с организатором

- [ ] Получен URL источника данных (ICS/API/RSS)
- [ ] Получен реферальный код или формат ссылки
- [ ] Уточнена частота обновления событий
- [ ] Проверена доступность источника (работает ли URL)
- [ ] Протестирована интеграция на тестовых данных
- [ ] Согласована частота обновления (чтобы не перегружать их сервер)

---

## 💡 Рекомендации

1. **Начните с ICS календаря** - это самый простой и стандартный вариант
2. **Уточните формат реферальной ссылки** - у разных организаторов могут быть разные системы
3. **Протестируйте на небольшом количестве событий** перед полной интеграцией
4. **Настройте мониторинг** - чтобы видеть, что события загружаются корректно
5. **Договоритесь о частоте обновления** - не стоит обновлять слишком часто (раз в час обычно достаточно)

---

## 🔍 Проверка работы интеграции

После добавления источника можно проверить:

```sql
-- Посмотреть все партнёрские реферальные коды
SELECT * FROM partner_referral_codes WHERE is_active = true;

-- Посмотреть события от конкретного источника
SELECT id, title, url, source, created_at_utc 
FROM events 
WHERE source LIKE 'ics.%' 
ORDER BY created_at_utc DESC 
LIMIT 10;

-- Проверить, что реферальный код добавлен
SELECT url FROM events 
WHERE url LIKE '%ref=%' 
ORDER BY created_at_utc DESC 
LIMIT 10;
```

---

## 📞 Контакты для вопросов

Если у организатора есть вопросы по интеграции, можно направить их к этому документу или связаться с технической поддержкой.

