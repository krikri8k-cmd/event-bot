# 📅 Анализ добавления фильтрации "Сегодня/Завтра" в функцию "Что рядом"

## 🔍 Текущая реализация

### Как работает сейчас:
1. **Функция поиска**: `search_events_today()` в `utils/unified_events_service.py`
2. **Фильтрация по дате**: 
   - Использует `get_today_start_utc(city)` и `get_tomorrow_start_utc(city)`
   - Фильтрует события: `WHERE starts_at >= :start_utc AND starts_at < :end_utc`
   - **Показывает только события на сегодня**

3. **UI кнопки**:
   - Пагинация: "◀️ Назад", "Вперёд ▶️", "Стр. X/Y"
   - Расширение радиуса: "🔍 Расширить до X км"
   - **Нет кнопок фильтрации по дате**

4. **Состояние пользователя**:
   - Сохраняется в `user_state[chat_id]`:
     - `prepared` - отфильтрованные события
     - `lat`, `lng`, `radius` - параметры поиска
     - `page` - текущая страница
   - **Нет сохранения выбранной даты**

## ✅ Что нужно сделать

### 1. Изменения в `search_events_today()` (или новая функция)
**Сложность: 🟢 Низкая**

Нужно добавить параметр `date_offset: int = 0`:
- `0` = сегодня (по умолчанию)
- `1` = завтра

```python
def search_events_today(
    self,
    city: str,
    user_lat: float | None = None,
    user_lng: float | None = None,
    radius_km: float = 15,
    date_offset: int = 0,  # НОВЫЙ ПАРАМЕТР
    message_id: str | None = None,
) -> list[dict]:
    # Вычисляем временные границы
    if date_offset == 0:
        start_utc = get_today_start_utc(city)
        end_utc = get_tomorrow_start_utc(city)
    elif date_offset == 1:
        start_utc = get_tomorrow_start_utc(city)
        end_utc = get_tomorrow_start_utc(city) + timedelta(days=1)
    # ... остальной код без изменений
```

**Альтернатива**: Создать функцию `get_date_start_utc(city, days_offset)` в `utils/simple_timezone.py`

### 2. Изменения в UI (кнопки)
**Сложность: 🟡 Средняя**

В функции `render_page()` (строка 1220) нужно добавить кнопки фильтрации:

**Текущая структура кнопок:**
```
[◀️ Назад] [Вперёд ▶️]
[Стр. 1/2]
[🔍 Расширить до 10 км]
[🔍 Расширить до 15 км]
[🔍 Расширить до 20 км]
```

**Новая структура:**
```
[◀️ Назад] [Вперёд ▶️]
[Стр. 1/2]
[📅 Сегодня] [📅 Завтра]  ← НОВЫЕ КНОПКИ (переключаются)
[🔍 Расширить до 10 км]
[🔍 Расширить до 15 км]
[🔍 Расширить до 20 км]
```

**Логика переключения:**
- По умолчанию показывается "📅 Сегодня" (активная) и "📅 Завтра" (неактивная)
- При нажатии на "📅 Завтра": она становится активной, "📅 Сегодня" - неактивной
- При нажатии на "📅 Сегодня": она становится активной, "📅 Завтра" - неактивной

**Callback data:**
- `date_filter:today` - показать сегодня
- `date_filter:tomorrow` - показать завтра

### 3. Сохранение состояния
**Сложность: 🟢 Низкая**

В `user_state[chat_id]` добавить:
```python
user_state[message.chat.id] = {
    "prepared": prepared,
    "counts": counts,
    "lat": lat,
    "lng": lng,
    "radius": int(radius),
    "page": 1,
    "date_filter": "today",  # НОВОЕ: "today" или "tomorrow"
    "diag": diag,
}
```

### 4. Обработчик callback для переключения даты
**Сложность: 🟡 Средняя**

Нужно создать обработчик:
```python
@main_router.callback_query(F.data.startswith("date_filter:"))
async def on_date_filter_change(callback: types.CallbackQuery):
    # Получаем выбранную дату
    date_type = callback.data.split(":")[1]  # "today" или "tomorrow"
    
    # Получаем состояние пользователя
    state = user_state.get(callback.message.chat.id)
    if not state:
        await callback.answer("❌ Состояние потеряно, начните поиск заново")
        return
    
    # Обновляем фильтр даты
    state["date_filter"] = date_type
    state["page"] = 1  # Сбрасываем страницу
    
    # Перезагружаем события с новым фильтром
    date_offset = 0 if date_type == "today" else 1
    events = events_service.search_events_today(
        city=state.get("city", "bali"),
        user_lat=state["lat"],
        user_lng=state["lng"],
        radius_km=state["radius"],
        date_offset=date_offset  # НОВЫЙ ПАРАМЕТР
    )
    
    # Пересоздаем prepared события
    # ... (аналогично текущей логике)
    
    # Перерисовываем страницу с новыми кнопками
    # ...
```

### 5. Парсеры и события
**Сложность: 🟢 Низкая (уже работает!)**

✅ **Хорошая новость**: Парсеры уже сохраняют события на сегодня и завтра!

- **BaliForum**: Парсит события с датами "Сегодня", "Завтра" и конкретными датами
- **KudaGo**: Получает события из API с правильными датами
- **События пользователей**: Могут быть созданы на любую дату

**Проверка в БД:**
- События хранятся в UTC в поле `starts_at`
- Фильтрация по дате работает через временные границы в UTC
- **Никаких изменений в парсерах не требуется!**

## 📊 Оценка сложности

| Задача | Сложность | Время | Риски |
|--------|-----------|-------|-------|
| Изменение `search_events_today()` | 🟢 Низкая | 30 мин | Минимальные |
| Добавление кнопок в UI | 🟡 Средняя | 1-2 часа | Нужно аккуратно разместить |
| Обработчик callback | 🟡 Средняя | 1-2 часа | Нужно перезагружать события |
| Сохранение состояния | 🟢 Низкая | 15 мин | Минимальные |
| Тестирование | 🟡 Средняя | 1-2 часа | Проверить все сценарии |

**Общая оценка: 🟡 Средняя сложность**
- **Время разработки**: 4-6 часов
- **Риски**: Средние (нужно аккуратно интегрировать с существующей пагинацией)

## ⚠️ Потенциальные проблемы

1. **Переключение между датами при пагинации**
   - Если пользователь на странице 2 и переключает дату, нужно сбросить на страницу 1
   - ✅ Решается: `state["page"] = 1` при переключении

2. **Сохранение состояния при расширении радиуса**
   - При расширении радиуса нужно сохранять выбранную дату
   - ✅ Решается: Добавить `date_filter` в состояние

3. **Разные часовые пояса**
   - "Сегодня" в Бали и "Сегодня" в Москве - разные даты
   - ✅ Уже решено: Используется `get_today_start_utc(city)` с учетом города

4. **Пустые результаты для "Завтра"**
   - Если событий на завтра нет, нужно показать понятное сообщение
   - ✅ Решается: Аналогично текущей логике для пустых результатов

## 💡 Рекомендации

### Вариант 1: Минимальные изменения (рекомендуется)
1. Добавить параметр `date_offset` в `search_events_today()`
2. Добавить кнопки "Сегодня/Завтра" в `render_page()`
3. Создать обработчик `on_date_filter_change()`
4. Сохранять `date_filter` в состоянии

**Плюсы**: Минимальные изменения, быстро реализовать
**Минусы**: Нужно перезагружать события при переключении

### Вариант 2: Оптимизация (для будущего)
- Загружать события на сегодня и завтра одновременно
- Переключать только отображение без перезагрузки

**Плюсы**: Быстрее переключение
**Минусы**: Больше изменений, больше памяти

## ✅ Вывод

**Добавление фильтрации "Сегодня/Завтра" - РЕАЛИЗУЕМО и НЕ СЛОЖНО!**

- ✅ Парсеры уже сохраняют события на завтра
- ✅ Фильтрация по дате уже работает (только для сегодня)
- ✅ Нужно добавить параметр `date_offset` и UI кнопки
- ✅ Оценка: 4-6 часов работы

**Рекомендация**: Начать с Варианта 1 (минимальные изменения), протестировать, затем при необходимости оптимизировать.

