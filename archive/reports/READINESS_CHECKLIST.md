# ✅ Чеклист готовности к работе над фильтрацией "Сегодня/Завтра"

## 📋 Проверка готовности

### ✅ 1. Точка восстановления
- [x] Git коммит создан: `2b428d9`
- [x] Git тег создан: `before-date-filter`
- [x] Рабочая директория чистая (no uncommitted changes)

**Команда для возврата:**
```bash
git reset --hard before-date-filter
```

### ✅ 2. Анализ выполнен
- [x] `DATE_FILTER_ANALYSIS.md` - полный анализ реализации
- [x] `RISKS_ANALYSIS.md` - анализ рисков и критических мест

### ✅ 3. Понимание логики подтверждено
- [x] Логика работы понята и подтверждена
- [x] Расположение кнопок определено
- [x] Поток данных понят

### ✅ 4. Ключевые файлы найдены
- [x] `utils/unified_events_service.py` - функция `search_events_today()` (строка 23)
- [x] `bot_enhanced_v3.py` - функция `kb_pager()` (строка 1254)
- [x] `bot_enhanced_v3.py` - обработчик геолокации (строка 3094)
- [x] `bot_enhanced_v3.py` - обработчик расширения радиуса (строка 4490)

### ✅ 5. Критические места определены
- [x] Обработчик расширения радиуса (`rx:`) - строка 4490
- [x] Обработчик расширения радиуса (`rx:`) - строка 4535
- [x] Обработчик пагинации (`pg:`) - строка 6809
- [x] Функция `kb_pager()` - строка 1254

## 📝 План работы

### Этап 1: Изменение `search_events_today()`
1. Добавить параметр `date_offset: int = 0` (по умолчанию 0 для обратной совместимости)
2. Изменить логику вычисления временных границ
3. Обновить SQL запросы

### Этап 2: Обновление `kb_pager()`
1. Добавить параметр `date_filter: str = "today"`
2. Добавить кнопки "📅 Сегодня" / "📅 Завтра"
3. Правильно разместить кнопки в клавиатуре

### Этап 3: Обработчик переключения даты
1. Создать `@main_router.callback_query(F.data.startswith("date_filter:"))`
2. Перезагрузить события с новым фильтром
3. Обновить состояние пользователя
4. Перерисовать сообщение с новыми кнопками

### Этап 4: Обновление всех мест использования
1. Обработчик геолокации (строка 3094) - добавить `date_filter="today"` в состояние
2. Обработчик расширения радиуса (строка 4490) - добавить `date_offset` из состояния
3. Обработчик расширения радиуса (строка 4535) - добавить `date_offset` из состояния
4. Обработчик пагинации (строка 6809) - передать `date_filter` в `kb_pager()`

### Этап 5: Тестирование
1. Проверить переключение "Сегодня" → "Завтра"
2. Проверить переключение "Завтра" → "Сегодня"
3. Проверить расширение радиуса при разных датах
4. Проверить пагинацию при разных датах
5. Проверить обратную совместимость (старые состояния)

## ⚠️ Важные правила безопасности

1. **Всегда использовать `.get("date_filter", "today")`** при чтении состояния
2. **Всегда передавать `date_offset`** при вызове `search_events_today()` (или использовать дефолт)
3. **Сбрасывать страницу на 1** при переключении даты
4. **Сохранять `date_filter`** в состояние при всех операциях

## 🎯 Готовность: 100%

**Все проверки пройдены. Можно начинать работу! ✅**

