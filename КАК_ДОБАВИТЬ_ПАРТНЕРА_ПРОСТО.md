# 📝 Как добавить партнёра с реферальным кодом (ПРОСТОЙ способ)

## ✅ Простой подход: колонка в таблице events

Вместо отдельной таблицы, мы просто добавляем колонки `referral_code` и `referral_param` прямо в таблицу `events`.

## Шаг 1: Применить миграцию

```sql
-- Выполните этот SQL в вашей базе данных
psql $DATABASE_URL -f sql/2025_add_referral_code_to_events.sql
```

Или вручную:

```sql
ALTER TABLE events ADD COLUMN IF NOT EXISTS referral_code TEXT;
ALTER TABLE events ADD COLUMN IF NOT EXISTS referral_param TEXT DEFAULT 'ref';

CREATE INDEX IF NOT EXISTS ix_events_referral_code 
    ON events (referral_code) 
    WHERE referral_code IS NOT NULL;
```

## Шаг 2: Настроить реферальный код для источника

Создайте простую таблицу для маппинга "URL источника → реферальный код":

```sql
-- Создать таблицу для маппинга (опционально, можно хранить в env переменных)
CREATE TABLE IF NOT EXISTS partner_referral_codes (
    source_url TEXT PRIMARY KEY,
    referral_code TEXT NOT NULL,
    referral_param TEXT DEFAULT 'ref',
    partner_name TEXT
);

-- Добавить реферальный код для партнёра
INSERT INTO partner_referral_codes (
    source_url, referral_code, referral_param, partner_name
) VALUES (
    'https://organizer.com/events.ics',
    'EVENTBOT2024',
    'ref',
    'Bali Yoga Studio'
)
ON CONFLICT (source_url) DO UPDATE SET
    referral_code = EXCLUDED.referral_code,
    referral_param = EXCLUDED.referral_param;
```

## Шаг 3: Добавить ICS календарь в переменные окружения

```bash
# В Railway или .env файле
ICS_FEEDS=https://organizer.com/events.ics
ENABLE_ICS_FEEDS=1
```

## Как это работает:

1. ✅ Система загружает события из ICS календаря
2. ✅ Автоматически ищет реферальный код в таблице `partner_referral_codes` по URL календаря
3. ✅ Сохраняет событие в `events` с:
   - `url` = оригинальный URL (без реферального кода)
   - `referral_code` = реферальный код (например: 'EVENTBOT2024')
   - `referral_param` = название параметра (например: 'ref')
4. ✅ При показе события пользователю, реферальный код добавляется к URL динамически

## Обновить реферальный код для всех событий источника:

```sql
-- Обновить реферальный код для всех событий от конкретного источника
UPDATE events 
SET referral_code = 'NEW_CODE',
    referral_param = 'affiliate'
WHERE source LIKE 'ics.%' 
  AND url LIKE '%organizer.com%';
```

## Проверка:

```sql
-- Посмотреть события с реферальными кодами
SELECT id, title, url, referral_code, referral_param, source 
FROM events 
WHERE referral_code IS NOT NULL 
ORDER BY created_at_utc DESC 
LIMIT 10;
```

---

**Преимущества этого подхода:**
- ✅ Проще - всё в одной таблице `events`
- ✅ Можно легко обновить реферальный код для всех событий
- ✅ Можно анализировать эффективность разных кодов
- ✅ URL остаётся чистым, реферальный код добавляется динамически
