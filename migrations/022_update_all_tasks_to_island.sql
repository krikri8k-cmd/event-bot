-- Обновление всех существующих заданий на островные (island)
-- Все задания больше подходят для Бали, чем для городов

UPDATE tasks 
SET task_type = 'island'
WHERE task_type = 'urban' OR task_type IS NULL;

-- Комментарий: все существующие задания помечены как island
-- Городские задания нужно будет создать отдельно

