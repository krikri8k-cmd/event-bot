# Аудит: парсер и переводы (GPT)

**Дата:** 2025-02-16  
**Задача:** прозрачная картина без изменений кода — когда запускается парсинг, кто вызывает ingest, где вызывается GPT, сохраняется ли перевод в БД, кэш.

---

## 1. PARSER FLOW

### Где создаётся scheduler

- **Класс:** `ModernEventScheduler` в `modern_scheduler.py` (стр. 22).
- **Создание экземпляра:**
  - `get_modern_scheduler()` (стр. 1034) — синглтон: при первом вызове создаётся `ModernEventScheduler()`, дальше возвращается тот же объект.
- **Кто вызывает старт:**
  - `start_modern_scheduler()` (стр. 1042) вызывается только при старте приложения:
    - **webhook_attach.py** (стр. 198–202): в `lifespan` после инициализации бота запускается поток `start_scheduler_thread()` → `start_modern_scheduler()`.
    - **start_production.py** (стр. 31): `start_modern_scheduler()` в потоке автоматизации.

Методов с именами `run_parser()` или `run_all_parsers()` в коде нет. Есть:
- `run_full_ingest()` — полный цикл (BaliForum + AI);
- `run_kudago_ingest()` — только KudaGo;
- `ParserIntegration.run_parsers_and_save()` в `utils/parser_integration.py` — **нигде не вызывается** (ни из бота, ни из планировщика).

### Когда запускается ingest

| Условие | Вызов |
|--------|--------|
| **При старте сервера** | **Да.** В `ModernEventScheduler.start()` (стр. 1011–1013) после `self.scheduler.start()` один раз вызывается `self.run_full_ingest()`. То есть при каждом холодном старте приложения (деплой, рестарт) сразу выполняется полный цикл BaliForum + AI. |
| **По расписанию (cron)** | **Да.** В `start()` добавляются джобы (стр. 891–934): |
| | **BaliForum + AI:** `run_full_ingest` — 18:02 UTC и 04:02 UTC (id: `modern-ingest-morning`, `modern-ingest-evening`). |
| | **KudaGo:** `run_kudago_ingest` — 21:02 UTC и 09:02 UTC (id: `kudago-ingest-morning`, `kudago-ingest-evening`). |
| **При запросе пользователя** | **Нет.** Обработка локации / «События рядом» только вызывает `events_service.search_events_today(...)` — чтение из БД, ingest не вызывается. |
| **При /start** | **Нет.** `cmd_start` (bot_enhanced_v3.py, стр. 3280) не вызывает ни ingest, ни планировщик. |
| **В webhook handler** | **Нет.** В webhook только диспетчеризация сообщений; ingest запускается один раз при старте приложения (в lifespan), не на каждый апдейт. |

### Расписание по парсерам

| Парсер | Джобы | Расписание (UTC) |
|--------|--------|-------------------|
| BaliForum | `modern-ingest-morning`, `modern-ingest-evening` | 18:02, 04:02 (2 раза в день) |
| AI events | Входят в `run_full_ingest` | то же 18:02, 04:02 |
| KudaGo | `kudago-ingest-morning`, `kudago-ingest-evening` | 21:02, 09:02 (2 раза в день) |

### Отдельный старый контур (ICS)

- **scheduler.py**: старый планировщик с `ingest_once()` по интервалу 5 минут (ICS/Nexudus).
- **api/admin.py**: `POST /admin/ingest/run` вызывает именно этот `ingest_once()` (стр. 69–72), **не** BaliForum/KudaGo. То есть ручной «запуск ingest» через админку триггерит только старый ICS-ингет.

---

## 2. GPT TRANSLATION FLOW

### Где используется GPT

- **Модель:** `gpt-4o-mini` (в `utils/event_translation.py`, стр. 23: `OPENAI_MODEL = "gpt-4o-mini"`).
- **Вызовы API:**
  - `client.chat.completions.create(...)` в двух местах в `event_translation.py`:
    - **Одиночный перевод:** `translate_event_to_english()` (стр. 116–125) — один запрос на одно событие (title + опционально description, location_name).
    - **Пакетный перевод заголовков:** `translate_titles_batch()` (стр. 302–309) — один запрос на список заголовков, возвращает массив переведённых строк.

### Где вызывается перевод

| Место | Когда | Что вызывается |
|-------|--------|------------------|
| **modern_scheduler.ingest_baliforum()** | При каждом запуске ingest BaliForum (по расписанию + раз при старте) | Сначала SELECT по `external_id, title_en` из `events` для уже сохранённых; в `to_translate` попадают только события, у которых **нет** `title_en`. Затем `translate_titles_batch(titles)` — один batch на все недостающие заголовки. Результаты передаются в `save_parser_event(..., title_en=title_en)`, GPT при сохранении **не** вызывается. |
| **utils/unified_events_service.save_parser_event()** | При сохранении парсерного события (BaliForum после batch, KudaGo и AI — по одному) | **Проверка:** если в БД уже есть запись с непустым `title_en` — перевод не вызывается, берутся значения из БД (стр. 542–546). Если передан `title_en` снаружи (batch BaliForum) — API не вызывается (стр. 536–541). Если записи нет или `title_en` пустой — вызывается `translate_event_to_english(title, description, location_name)` (стр. 547–552) и результат пишется в БД. |
| **utils/unified_events_service.create_user_event()** | При создании пользовательского события | После вставки события в БД в отдельном потоке вызывается `translate_event_to_english(...)` и результат записывается в таблицу через UPDATE (стр. 452–475). То есть перевод при создании пользовательского события — всегда по одному вызову GPT на событие. |

Итого:
- **BaliForum:** перевод при ingest — пакетно (`translate_titles_batch`), только для событий без `title_en` в БД; при сохранении в `save_parser_event` передаётся готовый `title_en`, GPT не вызывается.
- **KudaGo и AI:** при сохранении в `save_parser_event` **не** передаётся `title_en`; для новых/без перевода вызывается `translate_event_to_english()` **по одному запросу на каждое событие**.
- **Пользовательские события:** один вызов GPT на событие в фоне после создания.

### Когда вызывается перевод

- **Во время парсинга (ingest):** да — для BaliForum (batch до сохранения), для KudaGo и AI — при вызове `save_parser_event` во время ingest.
- **Во время выдачи пользователю:** нет — при показе событий только читаются поля `title_en`, `description_en`, `location_name_en` из БД, GPT не вызывается.
- **При первом EN-запросе:** нет — отдельной логики «перевести при первом показе на EN» нет; перевод делается при ingest/создании и при создании пользовательского события.

### Сохраняется ли перевод в БД

- **Да.** Результат перевода записывается в таблицу `events`:
  - В `save_parser_event`: через INSERT или UPDATE с полями `title_en`, `description_en`, `location_name_en` (unified_events_service.py, стр. 576–585 и 462–475).
  - В `create_user_event`: отдельный UPDATE по `event_id` с теми же полями (стр. 461–474).

### Кэширование

- **Проверка перед вызовом GPT:** да. В `save_parser_event` (стр. 516–546):
  - Сначала выполняется `SELECT id, title, description, title_en, description_en, location_name_en FROM events WHERE source = :source AND external_id = :external_id`.
  - Если запись есть и `title_en` непустой — берутся `title_en`, `description_en`, `location_name_en` из БД, **OpenAI не вызывается** (стр. 542–546: «В базе уже есть перевод — никогда не вызываем OpenAI повторно»).
  - GPT вызывается только если `need_translation == True`: не передан `title_en` снаружи и (записи нет или в БД нет `title_en`).
- **BaliForum:** дополнительно перед batch-переводом делается выборка по `external_id, title_en` по всем подходящим external_id; в batch попадают только те, у кого `title_en` пустой. То есть для уже переведённых событий GPT при следующем ingest не вызывается.

**Ответ на вопрос «если один и тот же event уже переведён, будет ли GPT вызван повторно?»** — **нет.** Участок кода: `utils/unified_events_service.py`, строки 516–546 (проверка `existing_row` и `existing_has_title_en`, ветка `elif existing_row and existing_has_title_en`).

---

## 3. DATABASE STRUCTURE

### Таблица events

- **Один `title`, один `description`, один `location_name`** — считаются основными (фактически RU).
- **Отдельные колонки для EN:** `title_en`, `description_en`, `location_name_en` (миграция 040).

Отдельных колонок `title_ru` / `description_ru` нет; «оригинал» — это `title`, `description`, `location_name`.

### Как хранятся языки

- RU: `title`, `description`, `location_name`.
- EN: `title_en`, `description_en`, `location_name_en`.
- Перевод **не перезаписывает** оригинал: оригинал остаётся в `title`/`description`/`location_name`, перевод — в `_en` полях.

### Проверка перед повторным переводом

- **Да.** В `save_parser_event` перед вызовом `translate_event_to_english` проверяется наличие записи и непустого `title_en`; при наличии перевод из БД переиспользуется, GPT не вызывается.
- В BaliForum перед batch проверяется по БД, у кого уже есть `title_en`; такие в `to_translate` не попадают.

### Fallback при отображении

- В боте (например, `render_event_html`): при `user_lang == 'en'` используется `title_en` или fallback на `title`; для описания — `description_en` или `description` (bot_enhanced_v3.py, стр. 1083–1096).

---

## 4. ЛОГИРОВАНИЕ

- Строк вида **"TRANSLATING EVENT"** или **"GPT CALL"** в коде нет.
- Есть:
  - **"🚀 ЗАПУСК ПЛАНОВОГО ОБНОВЛЕНИЯ ДАННЫХ"** (modern_scheduler.py, 554, 39, 371) — при старте `run_full_ingest`, ingest BaliForum, ingest KudaGo.
  - **"🌴 Запуск парсинга BaliForum..."** (39), **"🎭 Запуск парсинга KudaGo..."** (372).
  - В event_translation: **"event_translation: переведено title ..."** (DEBUG), **"event_translation: невалидный JSON от GPT"**, **"event_translation batch: переведено X из Y"** (334), предупреждения при ошибках и повторах.

Условия: логи ingest — при каждом запуске соответствующего метода по расписанию или при старте; логи перевода — при реальном вызове API (успех/ошибка/batch).

---

## 5. ПРОБЛЕМЫ (потенциальные архитектурные замечания)

1. **Ingest при каждом старте сервера**  
   В `start()` планировщика сразу после `scheduler.start()` вызывается `self.run_full_ingest()`. При частых рестартах (деплои, рестарты воркеров) полный цикл BaliForum + AI будет выполняться каждый раз, что даёт лишние запросы к источникам и к GPT (для событий без `title_en`).

2. **KudaGo и AI — по одному вызову GPT на событие**  
   В отличие от BaliForum, для KudaGo и AI не используется пакетный перевод: при каждом `save_parser_event` без переданного `title_en` вызывается `translate_event_to_english()` для одного события. При большом количестве новых событий за один ingest это даёт много последовательных (или ограниченных семафором) запросов к OpenAI.

3. **Два контура ingest**  
   Есть «современный» планировщик (BaliForum, KudaGo, AI) и старый (scheduler.py: ICS каждые 5 минут + ручной вызов через `POST /admin/ingest/run`). Админский «запуск ingest» не запускает BaliForum/KudaGo.

4. **Пользовательские события**  
   Перевод создаётся в фоновом потоке после создания события; при сбое API перевод может так и остаться пустым (в логах будет предупреждение), повторной автоматической попытки нет.

5. **Явного логирования «INGEST START» / «TRANSLATING EVENT»** нет — поиск по таким строкам ничего не даёт; ориентир — сообщения выше и логи `event_translation*`.

---

## 6. КРАТКИЕ ОТВЕТЫ НА ВОПРОСЫ ТЗ

- **Запускается ли ingest при старте сервера?** — Да, один раз вызывается `run_full_ingest()` (BaliForum + AI).
- **Запускается ли ingest при каждом пользовательском запросе?** — Нет.
- **Какие cron-расписания заданы?** — BaliForum+AI: 18:02 и 04:02 UTC; KudaGo: 21:02 и 09:02 UTC.
- **Где вызывается GPT?** — В `utils/event_translation.py` (`translate_event_to_english`, `translate_titles_batch`); из `unified_events_service.save_parser_event` и `create_user_event`, из `modern_scheduler.ingest_baliforum` (только batch заголовков).
- **Сохраняется ли перевод в БД?** — Да, в `title_en`, `description_en`, `location_name_en`.
- **Проверяется ли наличие EN перед повторным переводом?** — Да, в `save_parser_event` и при формировании списка для batch в BaliForum.
- **Будет ли GPT вызван повторно для уже переведённого события?** — Нет, при наличии непустого `title_en` в БД перевод не вызывается.

---

## 7. Дополнение: save_parser_event и отображение (ТЗ title_en)

### Этап 2 — Логика save_parser_event при existing_row и title_en NULL

- **Файл:** `utils/unified_events_service.py`, метод `save_parser_event`.
- **Условие:** `need_translation = not passed_en and (not existing_row or not existing_has_title_en)` (стр. 535).
- Если `existing_row` найден и `title_en` в БД пустой, то `existing_has_title_en` = False, значит `need_translation` = True → выполняется ветка `elif need_translation`, вызывается `translate_event_to_english(...)`. **Повторный перевод для существующих записей с пустым title_en не блокируется — GPT вызывается.** Дополнен комментарий в коде: «Новая запись или существующая с title_en NULL — вызываем перевод (догоняющий для старых)».

### Этап 6 — Отображение в боте

- **render_event_html** (bot_enhanced_v3.py, стр. 1076–1094): при `lang == "en"` используется `display_title = title_en if title_en else title_ru`, т.е. по сути `title_to_show = event.title_en or event.title`. При `lang != "en"` показывается `title`.
- **user_lang:** берётся из `get_user_language_or_default(user_id)` (стр. 1082), которая читает язык из БД: `get_user_language(user_id)` → `session.query(User).filter(User.id == user_id).first()` → `user.language_code` (utils/user_language.py, стр. 12–26, 66–79). То есть при EN всегда используется сохранённый в БД язык пользователя.
