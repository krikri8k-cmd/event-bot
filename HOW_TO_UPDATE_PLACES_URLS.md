# 📍 Как обновлять Google Maps ссылки в базе данных

## ⭐ Рекомендуемый способ: Через файл places_simple.txt

**Самый простой способ!** Просто добавь ссылки в файл `places_simple.txt` и запусти скрипт:

```bash
# Обновить существующие места и добавить новые
python scripts/add_places_from_simple_file.py places_simple.txt --update
```

**Как это работает:**
1. Добавь ссылки в `places_simple.txt` (в том же формате, что и для добавления новых мест)
2. Запусти скрипт с флагом `--update`
3. Скрипт автоматически:
   - Найдет места с такими же ссылками и обновит их координаты
   - Найдет места с такими же координатами и обновит ссылки
   - Добавит новые места, если их еще нет

**Пример формата в файле:**
```
# Категория: Тело, Тип: Кафе, Регион: Москва
body:cafe:moscow
https://maps.app.goo.gl/ZAvphnPXfgXxod67A
https://maps.app.goo.gl/ABC123
```

**Преимущества:**
- ✅ Не нужно знать ID места
- ✅ Автоматически извлекает координаты из любых ссылок (включая короткие)
- ✅ Можно обновить много мест за раз
- ✅ Просто редактировать текстовый файл

---

## Вариант 2: Через Railway UI (простой способ)

1. Зайди в Railway → твой проект → PostgreSQL
2. Открой таблицу `task_places`
3. Найди нужную запись (можно фильтровать по `id`, `name`, `region`)
4. Нажми на строку для редактирования
5. В поле `google_maps_url` вставь новую ссылку
6. **Важно:** После изменения ссылки нужно также обновить координаты `lat` и `lng`!

### Как получить координаты из ссылки:

Если ссылка содержит координаты напрямую (например, `https://maps.google.com/?q=55.7558,37.6176`):
- Координаты уже в ссылке: `lat=55.7558`, `lng=37.6176`

Если ссылка на конкретное место (например, `https://maps.app.goo.gl/...`):
- Используй скрипт ниже для автоматического извлечения координат

## Вариант 2: Через скрипт (рекомендуется)

Скрипт автоматически извлечет координаты из новой ссылки и обновит всё в БД.

### Обновить одно место:

```bash
python scripts/update_place_url.py <place_id> <new_google_maps_url>
```

**Пример:**
```bash
python scripts/update_place_url.py 1 https://maps.google.com/?q=55.7558,37.6176
```

### Обновить несколько мест из CSV:

1. Создай файл `places_to_update.csv`:
```csv
id,google_maps_url
1,https://maps.google.com/?q=55.7558,37.6176
2,https://maps.app.goo.gl/ABC123
3,https://www.google.com/maps/place/Кафе/@55.7558,37.6176,15z
```

2. Запусти скрипт:
```bash
python scripts/update_place_url.py --file places_to_update.csv
```

### Обновить также название места из URL:

Добавь флаг `--update-name`:
```bash
python scripts/update_place_url.py 1 https://maps.google.com/... --update-name
```

## 📋 Форматы Google Maps ссылок, которые поддерживаются:

✅ `https://maps.google.com/?q=55.7558,37.6176`  
✅ `https://maps.google.com/maps?q=55.7558,37.6176`  
✅ `https://www.google.com/maps/place/Название/@55.7558,37.6176,15z`  
✅ `https://goo.gl/maps/...` (короткие ссылки)  
✅ `https://maps.app.goo.gl/...` (короткие ссылки)  

## ⚠️ Важные моменты:

1. **Всегда обновляй координаты вместе со ссылкой!** Если меняешь ссылку вручную через Railway UI, не забудь обновить `lat` и `lng`.

2. **Проверяй ссылку после обновления** - открой её в браузере, убедись что она ведет на правильное место.

3. **Если скрипт не может извлечь координаты** - возможно, ссылка на адрес (не координаты). В этом случае:
   - Используй Google Maps для поиска места
   - Скопируй координаты из URL или кликни правой кнопкой на метку → "Что здесь?" → скопируй координаты
   - Обнови вручную через Railway UI

## 🔍 Как найти ID места:

1. Через Railway UI: открой таблицу `task_places`, посмотри колонку `id`
2. Через SQL запрос в Railway:
```sql
SELECT id, name, region, google_maps_url 
FROM task_places 
WHERE name LIKE '%Название%' 
   OR region = 'moscow';
```

## 💡 Советы:

- Если обновляешь много мест, используй CSV файл - это быстрее
- Скрипт автоматически проверяет, что ссылка валидная
- Скрипт показывает что изменилось (старые и новые значения)
- Если нужно обновить только координаты без изменения ссылки, используй Railway UI напрямую

